#include "pathfinding.h"

/**
 * a_star_graph -  searches for the shortest and fastest path from a
 * starting point to a target point in a graph.
 * @graph: is the given graph
 * @start: is the start vertex
 * @target: is the target vertex
 * Return: a queue with the shortest path to target
 */
queue_t *a_star_graph(graph_t *graph, vertex_t const *start,
					  vertex_t const *target)
{
	return (NULL);
}
